name: tochegando_motoboy
description: Aplicativo Tô Chegando Motoboy.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'  # ✅ Atualizado para Dart 3+

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ✅ Firebase (versões estáveis e integradas)
  firebase_core: ^3.5.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.0
  firebase_storage: ^12.3.0
  firebase_messaging: ^15.1.0

  # ✅ Google Maps e localização
  google_maps_flutter: ^2.7.0
  geolocator: ^10.1.0
  geocoding: ^3.0.0
  location: ^7.0.1
  flutter_google_places_hoc081098: ^1.0.0  # substituto estável para flutter_google_places
  google_api_headers: ^2.0.1

  # ✅ UI e utilitários
  modal_bottom_sheet: ^3.0.0
  flutter_spinkit: ^6.0.0
  percent_indicator: ^4.2.3
  provider: ^6.1.2
  flutter_rating_bar: ^4.0.1
  shared_preferences: ^2.3.2
  image_picker: ^1.1.1
  url_launcher: ^6.3.0
  intl: ^0.19.1
  http: ^1.2.2
  uuid: ^4.4.0
  get_it: ^7.7.0

  # ✅ Notificações
  flutter_local_notifications: ^17.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

  assets:
    - assets/
